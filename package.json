{
  "name": "eonsort",
  "version": "0.0.0-dev",
  "description": "Sort files into date-based folders",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "test": "vitest run",
    "test:e2e": "wdio run tests/e2e/wdio.conf.js",
    "gate": "npm run gate:fmt && npm run gate:clippy && npm run gate:test && npm run gate:upright && npm run gate:tagging && npm run gate:faces && npm run gate:audit",
    "gate:fmt": "cargo fmt --all --check",
    "gate:clippy": "cargo clippy --workspace --all-targets -- -D warnings",
    "gate:test": "npm run build && npm run check && npm test && cargo test --workspace",
    "gate:upright": "cargo clippy -p eonsort-core --features upright --all-targets -- -D warnings && cargo test -p eonsort-core --features upright",
    "gate:tagging": "cargo clippy -p eonsort-core --features tagging,quality --all-targets -- -D warnings && cargo test -p eonsort-core --features tagging,quality",
    "gate:faces": "cargo clippy -p eonsort-core --features faces --all-targets -- -D warnings && cargo test -p eonsort-core --features faces",
    "gate:audit": "cargo audit",
    "icon": "node scripts/make-icon.mjs src-tauri/icons/source.png && tauri icon src-tauri/icons/source.png",
    "tauri": "tauri",
    "samples": "node scripts/fetch-sample-pictures.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2.7.3",
    "@tauri-apps/plugin-opener": "^2.5.5"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.8",
    "@sveltejs/kit": "^2.70.3",
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "@tauri-apps/cli": "^2.11.5",
    "@types/node": "^26.6.2",
    "@wdio/cli": "^9.32.0",
    "@wdio/local-runner": "^9.32.0",
    "@wdio/mocha-framework": "^9.32.0",
    "@wdio/spec-reporter": "^9.32.0",
    "edgedriver": "^6.3.0",
    "svelte": "^5.57.1",
    "svelte-check": "^4.7.6",
    "typescript": "~6.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  },
  "overrides": {
    "cookie": "^0.7.2",
    "serialize-javascript": "^7.1.0",
    "nanoid": "^3.3.18",
    "deepmerge-ts": "^8.0.1"
  }
}
